{
  "name": "atc-com-sim",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "validate": "node scripts/validate-search.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
